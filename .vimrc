" bind ctrl-c and ctrl-v to copying from systems clipboard and pasting from systems clipboard respectively using special + register
vnoremap <C-c> "+y
map <C-v> "+P

" syntax on by default
syntax on

" line numbers
set number

" enable mouse support
set mouse=a

